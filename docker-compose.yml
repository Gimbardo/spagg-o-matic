version: "3.9"
services:
  spaggomatic:
    build: .
    ports:
      - 3001:80
    environment:
      REDIS_ADDRESS: redis
  redis:
    image: redis
    command: redis-server
    volumes:
      - "./data:/data"